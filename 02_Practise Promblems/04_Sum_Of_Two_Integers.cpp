#include <iostream>
using namespace std;

int main() {
    int a, b;
    
    cin >> a >> b;           // Read two numbers
    int sum = a + b;         // Calculate sum
    cout << sum << endl;     // Print result
    
    return 0;
}